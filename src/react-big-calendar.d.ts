declare module "react-big-calendar" {
  const Calendar: any; // You can provide more specific types if available
  const momentLocalizer: any; // You can provide more specific types if available
  export { Calendar, momentLocalizer };
}
